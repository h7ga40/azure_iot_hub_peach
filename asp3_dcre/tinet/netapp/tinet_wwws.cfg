/*
 *  TINET (TCP/IP Protocol Stack)
 * 
 *  Copyright (C) 2001-2017 by Dep. of Computer Science and Engineering
 *                   Tomakomai National College of Technology, JAPAN
 *
 *  上記著作権者は，以下の (1)～(4) の条件か，Free Software Foundation 
 *  によって公表されている GNU General Public License の Version 2 に記
 *  述されている条件を満たす場合に限り，本ソフトウェア（本ソフトウェア
 *  を改変したものを含む．以下同じ）を使用・複製・改変・再配布（以下，
 *  利用と呼ぶ）することを無償で許諾する．
 *  (1) 本ソフトウェアをソースコードの形で利用する場合には，上記の著作
 *      権表示，この利用条件および下記の無保証規定が，そのままの形でソー
 *      スコード中に含まれていること．
 *  (2) 本ソフトウェアを，ライブラリ形式など，他のソフトウェア開発に使
 *      用できる形で再配布する場合には，再配布に伴うドキュメント（利用
 *      者マニュアルなど）に，上記の著作権表示，この利用条件および下記
 *      の無保証規定を掲載すること．
 *  (3) 本ソフトウェアを，機器に組み込むなど，他のソフトウェア開発に使
 *      用できない形で再配布する場合には，次の条件を満たすこと．
 *    (a) 再配布に伴うドキュメント（利用者マニュアルなど）に，上記の著
 *        作権表示，この利用条件および下記の無保証規定を掲載すること．
 *  (4) 本ソフトウェアの利用により直接的または間接的に生じるいかなる損
 *      害からも，上記著作権者およびTOPPERSプロジェクトを免責すること．
 *
 *  本ソフトウェアは，無保証で提供されているものである．上記著作権者お
 *  よびTOPPERSプロジェクトは，本ソフトウェアに関して，その適用可能性も
 *  含めて，いかなる保証も行わない．また，本ソフトウェアの利用により直
 *  接的または間接的に生じたいかなる損害に関しても，その責任を負わない．
 * 
 *  @(#) $Id$
 */

/*
 *  WWW サーバの TCP/IP コンフィギュレーションファイル
 *
 *    SUPPORT_INET6
 *    SUPPORT_INET4
 *    USE_TCP_NON_BLOCKING: OFF
 *    USE_TCP_EXTENTIONS:   OFF
 *    TCP 受付口
 *    TCP 通信端点
 * 
 */

#if defined(USE_WWW_SRV)

/*
 * TCP 受付口
 */

#if defined(USE_TCP_EXTENTIONS)

#else	/* of #if defined(USE_TCP_EXTENTIONS) */

#if defined(SUPPORT_INET6)

TCP6_CRE_REP (WWW_SRV_REPID, {
	0,
	{ IPV6_ADDRANY, 80 }
	} );

#if defined(SUPPORT_INET4) && defined(USE_WWW4_SRV)

TCP_CRE_REP (WWW4_SRV_REPID, {
	0,
	{ IPV4_ADDRANY, 80 }
	} );

#endif	/* of #if defined(SUPPORT_INET4) && defined(USE_WWW4_SRV) */

#else	/* of #if defined(SUPPORT_INET6 */

#if defined(SUPPORT_INET4)

TCP_CRE_REP (WWW_SRV_REPID, {
	0,
	{ IPV4_ADDRANY, 80 }
	} );

#endif	/* of #if defined(SUPPORT_INET4 */

#endif	/* of #if defined(SUPPORT_INET6 */

#endif	/* of #if defined(USE_TCP_EXTENTIONS */

/*
 * TCP 通信端点
 *
 * 定義する TCP 通信端点（WWW?_CEPID1）の選択
 *
 *   INET6 INET4 USE_TCP4  TCP6_CRE_CEP TCP_CRE_CEP
 *     ○    ○     ○          ×         WWW4
 *     ○    ○     ×          WWW        ×
 *     ○    ×     ○          WWW        ×
 *     ○    ×     ×          WWW        ×
 *     ×    ○     ○          ×         WWW
 *     ×    ○     ×          ×         WWW
 *     ×    ×     ○          ×         ×
 *     ×    ×     ×          ×         ×
 *
 */

#if defined(USE_TCP_EXTENTIONS)

#else	/* of #if defined(USE_TCP_EXTENTIONS */

#if defined(SUPPORT_INET6)

#if defined(SUPPORT_INET4)

#if defined(USE_WWW4_SRV)

#if defined(TCP_CFG_SWBUF_CSAVE)

#if defined(TCP_CFG_RWBUF_CSAVE)

TCP_CRE_CEP (WWW4_SRV_CEPID1, {
	0,
	NADR,
	WWW_SRV_SWBUF_SIZE,
	NADR,
	WWW_SRV_RWBUF_SIZE,
	NULL
	} );

#else	/* of #if defined(TCP_CFG_RWBUF_CSAVE) */

TCP_CRE_CEP (WWW4_SRV_CEPID1, {
	0,
	NADR,
	WWW_SRV_SWBUF_SIZE,
	www_srv_rwbuf[0],
	WWW_SRV_RWBUF_SIZE,
	NULL
	} );

#endif	/* of #if defined(TCP_CFG_RWBUF_CSAVE) */

#else	/* of #if defined(TCP_CFG_SWBUF_CSAVE) */

#if defined(TCP_CFG_RWBUF_CSAVE)

TCP_CRE_CEP (WWW4_SRV_CEPID1, {
	0,
	www_srv_swbuf[0],
	WWW_SRV_SWBUF_SIZE,
	NADR,
	WWW_SRV_RWBUF_SIZE,
	NULL
	} );

#else	/* of #if defined(TCP_CFG_RWBUF_CSAVE) */

TCP_CRE_CEP (WWW4_SRV_CEPID1, {
	0,
	www_srv_swbuf[0],
	WWW_SRV_SWBUF_SIZE,
	www_srv_rwbuf[0],
	WWW_SRV_RWBUF_SIZE,
	NULL
	} );

#endif	/* of #if defined(TCP_CFG_RWBUF_CSAVE) */

#endif	/* of #if defined(TCP_CFG_SWBUF_CSAVE) */

#else	/* of #if defined(USE_WWW4_SRV) */

#if defined(TCP_CFG_SWBUF_CSAVE)

#if defined(TCP_CFG_RWBUF_CSAVE)

TCP6_CRE_CEP (WWW_SRV_CEPID1, {
	0,
	NADR,
	WWW_SRV_SWBUF_SIZE,
	NADR,
	WWW_SRV_RWBUF_SIZE,
	NULL
	} );

#else	/* of #if defined(TCP_CFG_RWBUF_CSAVE) */

TCP6_CRE_CEP (WWW_SRV_CEPID1, {
	0,
	NADR,
	WWW_SRV_SWBUF_SIZE,
	www_srv_rwbuf[0],
	WWW_SRV_RWBUF_SIZE,
	NULL
	} );

#endif	/* of #if defined(TCP_CFG_RWBUF_CSAVE) */

#else	/* of #if defined(TCP_CFG_SWBUF_CSAVE) */

#if defined(TCP_CFG_RWBUF_CSAVE)

TCP6_CRE_CEP (WWW_SRV_CEPID1, {
	0,
	www_srv_swbuf[0],
	WWW_SRV_SWBUF_SIZE,
	NADR,
	WWW_SRV_RWBUF_SIZE,
	NULL
	} );

#else	/* of #if defined(TCP_CFG_RWBUF_CSAVE) */

TCP6_CRE_CEP (WWW_SRV_CEPID1, {
	0,
	www_srv_swbuf[0],
	WWW_SRV_SWBUF_SIZE,
	www_srv_rwbuf[0],
	WWW_SRV_RWBUF_SIZE,
	NULL
	} );

#endif	/* of #if defined(TCP_CFG_RWBUF_CSAVE) */

#endif	/* of #if defined(TCP_CFG_SWBUF_CSAVE) */

#endif	/* of #if defined(USE_WWW4_SRV) */

#else	/* of #if defined(SUPPORT_INET4 */

#if defined(TCP_CFG_SWBUF_CSAVE)

#if defined(TCP_CFG_RWBUF_CSAVE)

TCP6_CRE_CEP (WWW_SRV_CEPID1, {
	0,
	NADR,
	WWW_SRV_SWBUF_SIZE,
	NADR,
	WWW_SRV_RWBUF_SIZE,
	NULL
	} );

#else	/* of #if defined(TCP_CFG_RWBUF_CSAVE) */

TCP6_CRE_CEP (WWW_SRV_CEPID1, {
	0,
	NADR,
	WWW_SRV_SWBUF_SIZE,
	www_srv_rwbuf[0],
	WWW_SRV_RWBUF_SIZE,
	NULL
	} );

#endif	/* of #if defined(TCP_CFG_RWBUF_CSAVE) */

#else	/* of #if defined(TCP_CFG_SWBUF_CSAVE) */

#if defined(TCP_CFG_RWBUF_CSAVE)

TCP6_CRE_CEP (WWW_SRV_CEPID1, {
	0,
	www_srv_swbuf[0],
	WWW_SRV_SWBUF_SIZE,
	NADR,
	WWW_SRV_RWBUF_SIZE,
	NULL
	} );

#else	/* of #if defined(TCP_CFG_RWBUF_CSAVE) */

TCP6_CRE_CEP (WWW_SRV_CEPID1, {
	0,
	www_srv_swbuf[0],
	WWW_SRV_SWBUF_SIZE,
	www_srv_rwbuf[0],
	WWW_SRV_RWBUF_SIZE,
	NULL
	} );

#endif	/* of #if defined(TCP_CFG_RWBUF_CSAVE) */

#endif	/* of #if defined(TCP_CFG_SWBUF_CSAVE) */

#endif	/* of #if defined(SUPPORT_INET4 */

#if NUM_WWW_SRV_TASKS >= 2

#if defined(TCP_CFG_SWBUF_CSAVE)

#if defined(TCP_CFG_RWBUF_CSAVE)

TCP6_CRE_CEP (WWW_SRV_CEPID2, {
	0,
	NADR,
	WWW_SRV_SWBUF_SIZE,
	NADR,
	WWW_SRV_RWBUF_SIZE,
	NULL
	} );

#else	/* of #if defined(TCP_CFG_RWBUF_CSAVE) */

TCP6_CRE_CEP (WWW_SRV_CEPID2, {
	0,
	NADR,
	WWW_SRV_SWBUF_SIZE,
	www_srv_rwbuf[1],
	WWW_SRV_RWBUF_SIZE,
	NULL
	} );

#endif	/* of #if defined(TCP_CFG_RWBUF_CSAVE) */

#else	/* of #if defined(TCP_CFG_SWBUF_CSAVE) */

#if defined(TCP_CFG_RWBUF_CSAVE)

TCP6_CRE_CEP (WWW_SRV_CEPID2, {
	0,
	www_srv_swbuf[1],
	WWW_SRV_SWBUF_SIZE,
	NADR,
	WWW_SRV_RWBUF_SIZE,
	NULL
	} );

#else	/* of #if defined(TCP_CFG_RWBUF_CSAVE) */

TCP6_CRE_CEP (WWW_SRV_CEPID2, {
	0,
	www_srv_swbuf[1],
	WWW_SRV_SWBUF_SIZE,
	www_srv_rwbuf[1],
	WWW_SRV_RWBUF_SIZE,
	NULL
	} );

#endif	/* of #if defined(TCP_CFG_RWBUF_CSAVE) */

#endif	/* of #if defined(TCP_CFG_SWBUF_CSAVE) */

#endif	/* of #if NUM_WWW_SRV_TASKS >= 2 */

#elif defined(SUPPORT_INET4)	/* of #if defined(SUPPORT_INET6) */

#if defined(TCP_CFG_SWBUF_CSAVE)

#if defined(TCP_CFG_RWBUF_CSAVE)

TCP_CRE_CEP (WWW_SRV_CEPID1, {
	0,
	NADR,
	WWW_SRV_SWBUF_SIZE,
	NADR,
	WWW_SRV_RWBUF_SIZE,
	NULL
	} );

#else	/* of #if defined(TCP_CFG_RWBUF_CSAVE) */

TCP_CRE_CEP (WWW_SRV_CEPID1, {
	0,
	NADR,
	WWW_SRV_SWBUF_SIZE,
	www_srv_rwbuf[0],
	WWW_SRV_RWBUF_SIZE,
	NULL
	} );

#endif	/* of #if defined(TCP_CFG_RWBUF_CSAVE) */

#else	/* of #if defined(TCP_CFG_SWBUF_CSAVE) */

#if defined(TCP_CFG_RWBUF_CSAVE)

TCP_CRE_CEP (WWW_SRV_CEPID1, {
	0,
	www_srv_swbuf[0],
	WWW_SRV_SWBUF_SIZE,
	NADR,
	WWW_SRV_RWBUF_SIZE,
	NULL
	} );

#else	/* of #if defined(TCP_CFG_RWBUF_CSAVE) */

TCP_CRE_CEP (WWW_SRV_CEPID1, {
	0,
	www_srv_swbuf[0],
	WWW_SRV_SWBUF_SIZE,
	www_srv_rwbuf[0],
	WWW_SRV_RWBUF_SIZE,
	NULL
	} );

#endif	/* of #if defined(TCP_CFG_RWBUF_CSAVE) */

#endif	/* of #if defined(TCP_CFG_SWBUF_CSAVE) */

#if NUM_WWW_SRV_TASKS >= 2

#if defined(TCP_CFG_SWBUF_CSAVE)

#if defined(TCP_CFG_RWBUF_CSAVE)

TCP_CRE_CEP (WWW_SRV_CEPID2, {
	0,
	NADR,
	WWW_SRV_SWBUF_SIZE,
	NADR,
	WWW_SRV_RWBUF_SIZE,
	NULL
	} );

#else	/* of #if defined(TCP_CFG_RWBUF_CSAVE) */

TCP_CRE_CEP (WWW_SRV_CEPID2, {
	0,
	NADR,
	WWW_SRV_SWBUF_SIZE,
	www_srv_rwbuf[1],
	WWW_SRV_RWBUF_SIZE,
	NULL
	} );

#endif	/* of #if defined(TCP_CFG_RWBUF_CSAVE) */

#else	/* of #if defined(TCP_CFG_SWBUF_CSAVE) */

#if defined(TCP_CFG_RWBUF_CSAVE)

TCP_CRE_CEP (WWW_SRV_CEPID2, {
	0,
	www_srv_swbuf[1],
	WWW_SRV_SWBUF_SIZE,
	NADR,
	WWW_SRV_RWBUF_SIZE,
	NULL
	} );

#else	/* of #if defined(TCP_CFG_RWBUF_CSAVE) */

TCP_CRE_CEP (WWW_SRV_CEPID2, {
	0,
	www_srv_swbuf[1],
	WWW_SRV_SWBUF_SIZE,
	www_srv_rwbuf[1],
	WWW_SRV_RWBUF_SIZE,
	NULL
	} );

#endif	/* of #if defined(TCP_CFG_RWBUF_CSAVE) */

#endif	/* of #if defined(TCP_CFG_SWBUF_CSAVE) */

#endif	/* of #if NUM_WWW_SRV_TASKS >= 2 */

#endif	/* of #if defined(SUPPORT_INET6) */

#endif	/* of #if defined(USE_TCP_EXTENTIONS) */

#endif	/* of #if defined(USE_WWW_SRV) */
